# AI 开发协作规则 · xinhua-hub

> **一句话定位**：这是新华保险资料库网站（xinhua-hub）的**强制协作规则**。
> 任何 AI（含跨账号接手的小龙虾 / 大龙虾 / 腾讯龙虾）修改本项目，**必须**先读本文，并逐条遵守。
> 最后修订：2026-08-05，依据 2026-06-30 ~ 2026-08-05 全部开发过程与踩坑教训整理。

---

## 0. 先找到家（开工前必读）

| 项 | 值 |
|----|-----|
| 本地源码（唯一源仓库） | `E:\workbuddyFIle\腾讯龙虾的成品\xinhua-hub\` |
| 线上地址 | https://1131921527-alt.github.io/xinhua-hub/ （已公开，无密码） |
| GitHub | https://github.com/1131921527-alt/xinhua-hub （分支 `main`） |
| 样式唯一来源 | `assets/css/theme.css`（设计 token）+ `assets/css/calc-common.css`（演算器公共样式） |
| 验收脚本 | `qa/capture_acceptance.py`（手机 `390×844` + 桌面 `1280×900`，查横向溢出/Console） |
| 每日验收报告 | `qa/daily-report/YYYY-MM-DD-report.md` |
| 详细接手文档 | `腾讯龙虾的成品\memory\detail\012-新华资料库的制作.md` |

**改样式只改一处**：品牌色 / 圆角 / 阴影 / 字体 / 间距，只编辑 `theme.css` 的 `:root` 变量；演算器公共样式（`.field` `.btn-dl` `.plan-table` `.key-metrics` 等）统一在 `calc-common.css`。不要逐个 HTML 内联重复改，也不要"顺手"重写整页。

---

## 核心架构原则（理解它，再动手）

本项目是**两层分离**结构，任何改动都不能模糊这条线：

1. **① 演算页 = 交互计算器**（`calculator-*.html`）
   保持可输入参数、点"生成"、实时算表。不要整页改成静态展示图。
2. **② 导出 / 下载图片 = 计划书版式**
   通过独立导出模板（如 `#exportRoot` / `#planArea`）生成，用 `html2canvas` 截图 → 手机/微信弹预览长按保存。版式严格匹配用户认定的"标准版"。

> 配色铁律（来自用户原话）：**"颜色换成蓝色以外，其他都不变。"**
> 即：原图红 → 蓝，其余结构 / 文案 / 数据原封不动。未经允许不得改结构、改文案、改数据。

---

## 八条强制规则

### 规则一 · 修改前必须理解现有结构，不允许直接重构

- 动手前，先用 `Grep` / `Read` 把目标文件和相关引用（CSS 类、`<script>` 块、`data.json` 入口）摸清楚。
- **禁止"整体重建大文件"式重构**。历史教训：用脚本把 3 个旧框架文件转新框架后，`generate()` 里残留失效的 `getElementById('results')`，导致脚本抛异常被 `catch` 吃掉、**表格永远空白**。修法永远是"精准删失效引用 + 最小结构对齐"，不是全量重写。
- 改一个演算器，先看它是否复用了 `calc-common.css` 的公共类，优先在公共层改，避免每个页面各改一遍又改出差异。
- `thead <th>` 列数校验要排除 `<thead` 标签本身（正则 `<th` 会误匹配 `<thead` 多算 1）；列对齐公式：`thead <th>` 数 == 每行数据 `<td>` 数（含侧标）。

### 规则二 · 不改变业务计算逻辑

- **业务计算逻辑（收益率公式、现金价值口径、红利领取方式、定存组合算法等）一律冻结**，不在"优化""重构""清理"之名下改动。
- 用户习惯中明确：**"修改计算公式"属于四大必须暂停确认项之一**，动了先问，不要直接提交。
- 数据口径：演算器数据来自已校验的 `*.json`（如 `hongtai_full.json` → `hongtai_data.js`、`data_yingmanxin.json`）。替换数据前必须拿官方演示表逐项核对，**不能改算法去迁就数据，也不能改数据去迁就算法**。
- 已踩的坑：原 `CALC_DATA_INLINE` 数据缩放/口径错误（"错到姥姥家"），修复方式是**换正确数据**，不是改计算逻辑。

### 规则三 · 不随意新增用户没有要求的模块

- 用户没点名要的页面、入口、统计项、二级菜单，**一律不做**。
- 历史教训：招行版曾误带"第二年末现价百分比"（实际是中行版特有），用户判定招行不需要 → 被移除。新增任何统计项 / 栏目前先确认它属于哪个渠道版本。
- 入口改动牵动 `data.json`（`categories`/`items`）、`index.html`、相关 `*.html` 三处，新增前想清楚三处怎么联动，别加一半。

### 规则四 · 优先级排序（冲突时按此裁决）

**手机端体验 > 图片导出体验 > PC 端体验**

- 当三者冲突，先保手机端，再保导出图，PC 端优先级最低。
- 例：某个桌面特性会撑破手机布局 → 砍掉桌面特性，保手机。某个 PC 展示效果会拖垮导出图清晰度 → 保导出图。
- 手机端判定标准：视口 `390×844, is_mobile=True`，**横向溢出必须为 0**（验收脚本硬性 FAIL 条件：`scrollWidth > innerWidth + 2`）。
- 标签横滑、字号、按钮热区，都以"招行私行客户经理在微信里点得动"为第一目标。

### 规则五 · 在线测算核心要求：网页效果 = 下载图片效果

- 屏幕上看到的计划书表格，和下载的 PNG 图片，**数据、结构、关键样式必须一致**，不能有"网页好看、图片缺一块"或反过来。
- 实现方式：导出图走**独立导出模板**（不截当前网页界面），模板是网页计划书的可控镜像。改了屏幕上计划表的任意一处（列、合并、高亮、免责话术），**必须同步改导出模板**，并用验收脚本同时截"在线页"和"导出 PNG"对比。
- 验收钩子：导出图通过给 `HTMLCanvasElement.prototype.toBlob` 打补丁把 blob 存 `window.__capBlob` 截获；`run_calc` 需等 `#kmMaturity` 文本非「—」让异步指标卡加载完，否则导出空白。
- 注入 JS 时**禁止选 `} catch` 这类跨子句锚点**，否则拆断 catch 会让整个 `<script>` 解析失败（宏御 hongyu 曾因此 `generate` 失效、导出 no blob）。

### 规则六 · 计划书图片硬性标准

导出 PNG 必须同时满足：

- **清晰**：导出宽度 ≥ `1600px`（scale=2）；盈满鑫类高密度表用 `1500px × scale 3 = 4500px`。`maxCanvasPixels` 不够先提上限（如 32M），别改表格布局去迁就。
- **适合微信发送**：单张长图，手机预览层长按即可保存；不要依赖 `showSaveFilePicker`（移动端/微信不支持），统一走预览层 `<img>` + blob 长按保存。
- **表格完整**：不截断、不丢行；适用产品要含完整演示表（如 56 年），前 5 年"储备期"合并显示 1 个并居中。
- **字体居中**：计划书表格单元格 `text-align:center; vertical-align:middle`，数值列、表头、合并单元格均居中。
- **边框统一**：统一描边风格（深蓝 `2px` / `1.5px` 蓝框），**不能出现某列或某单元格缺右边框**（盈满鑫"预期年化单利"列储备期曾因 `border-right:none` 多处叠加导致缺框，已修）。改边框要么全有要么全无，不要留半截。

### 规则七 · 产品话术：合规不能删掉销售真实场景

- **核心红线**：不能因为"合规"就删除销售实际在用的场景与话术。合规是"表述规范"，不是"砍内容"。
- 必须区分两个区域：
  - **正式页面内容**（对外 / 监管可见，如产品介绍页、条款页、门户）：用中性合规表述——"合同保证利益""分红利益具不确定性，以计划书演示为准"。**不写**"保证收益 / 固定收益"等绝对化数字表述。
  - **销售辅助话术**（内部使用，如 `sales-qa.html` 的"销售话术规范"板块）：保留真实使用场景、真实沟通表达。可含具体话术，但标注"内部使用 / 以计划书演示为准"。
- 禁用表述（"保证收益多少 / 一定达到 / 稳赚不赔"）**只作为 ⚠ 警示框举例**，不要因此把正文的真实销售场景整段删掉。
- 两类内容互补不重复：合规问答（无收益率数字）与内部解读（带数字）是两层，分别服务对外与对内（参考盈满鑫 1.0 段 vs `intro-yingmanxin.html`）。

### 规则八 · 修改后必须汇报（三要素缺一不可）

每次改动完成，必须向用户说清楚：

1. **修改了哪些文件**（逐文件列出路径）。
2. **改了什么内容**（逐文件摘要，含关键参数 / 样式 / 逻辑变化）。
3. **是否影响其他页面**（列出受波及的页面与模块，说明是否需要回归；如"改 `calc-common.css` 影响全部演算器，已用验收脚本全量回归"）。

汇报格式建议（也写进 `qa/daily-report/YYYY-MM-DD-report.md`）：

```markdown
## 修改文件
- calculator-yingmanxin.html（唯一变更）
- assets/css/calc-common.css（公共样式）

## 修改内容
- 储备期单元格由逐行 td 改回 rowspan 合并居中（仅显示 1 个"储备期"）
- 组合年化单利公式改为按交费年度加权平均

## 影响其他页面？
- 是：calc-common.css 影响全部演算器 → 已用 qa/capture_acceptance.py 全量回归，PC+手机溢出 0、Console 0
- commit：xxxxxxx，已 push origin main
- 线上：https://1131921527-alt.github.io/xinhua-hub/calculator-yingmanxin.html
```

---

## 标准版样式基准（视觉不可随意改的依据）

用户认定的"标准版本"，改样式时照此，**不擅自变体**：

- **深色标题底**：增额寿险系 `#1E3A8A`（宏御/宏泰/福盛/华彩/宏坤），养老年金系 `#78350F`（宏愿/恒享）。
- **左侧竖排标签**（`writing-mode: vertical-rl`）：增额系两列"不再缴费/长期收益"；年金系四列"不再缴费/长期收益/安全稳健/条款明确"。标签在**最左列**。
- **每 5 年高亮** + 末年高亮。
- **缴费 / 储备期合并**：前 X 年合并显示，储备期用"储备期"字样占位（招行版统一显示"储备期"，禁用"缴费期/投资期/缴费期分X年投入"）。
- **底部统一免责话术**："本利益演示基于本公司精算假设，不代表实际收益……具体以保险合同为准"。
- **副标题格式**："40岁女士 每年100万 缴3年 分红交清增额"（增额系）/ "46岁女士 每年50万 缴3年 55岁开始领"（年金系）。
- **已删除项（不要再复活）**：displayMode 选择器、emoji、旧浮动免责、交替行背景、rate-reserve 储备期独立列。

默认参数（已对齐标准版，改数据别动这些）：宏御 40女/100万/3年；宏泰 40女/100万/3年；宏愿 46女/50万/3年/55岁领；华彩 40女/100万/3年；宏坤 女/100万/3年；福盛 40女/100万/3年；恒享 46女/50万/3年/52岁领。

---

## 收尾自检清单（提交前过一遍）

- [ ] 改样式只动了 `theme.css` / `calc-common.css`，没有逐页内联重复改？
- [ ] 没有整体重建大文件，只做了最小精准改动？
- [ ] 业务计算逻辑原封未动（或已暂停向用户确认）？
- [ ] 没有新增用户没要求的模块 / 入口 / 统计项？
- [ ] 手机端 `390×844` 横向溢出 = 0？
- [ ] 在线页与导出 PNG 数据/结构/关键样式一致？
- [ ] 导出图：清晰、微信可发、表格完整、字体居中、边框统一？
- [ ] 销售真实场景未被"合规"误删；正式内容 / 销售话术已分区？
- [ ] 已用 `qa/capture_acceptance.py` 验收（PC+手机，Console=0，横向溢出=0）？
- [ ] 已汇报：修改文件 / 修改内容 / 是否影响其他页面？
- [ ] 已写 `qa/daily-report/YYYY-MM-DD-report.md` 并 `git push origin main`？

> **记住**：GitHub Pages 推送后约等 45 秒重建，否则看到旧版 / 404。微信内置浏览器会拦截 `github.io`，验收用 Chrome 桌面端。
