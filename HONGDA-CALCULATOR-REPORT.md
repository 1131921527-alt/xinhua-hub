# 宏达人生年金保险（分红型）演算器 · 接入与统一显示规则汇报

生成时间：2026-08-24

## 一、GitHub 状态（四个状态，如实汇报）

| 状态 | 结论 |
|------|------|
| 本地完成 | ✅ 已完成（代码已写入本地磁盘） |
| 已 commit | ❌ 未提交（工作区存在大量未跟踪/已修改文件，尚未 `git commit`） |
| 已 push | ❌ 未推送（`origin` 指向 `https://github.com/1131921527-alt/xinhua-hub.git`，当前分支 `main`） |
| GitHub Pages 已上线 | ❌ 未更新（仓库远端仍为旧内容，需先 commit 再 push，Pages 才会重新构建） |

> 说明：本轮所有改动都在本地完成，尚未进入版本库。推送与 Pages 上线属于「影响共享状态、不可逆」的操作，未经你确认不会自动执行。如需我帮你 commit + push，请明确授权。

## 二、宏达人生接入进度

### 1. 双正式入口
- **入口 A — 首页「在线测算」**：`index.html` 的「新华主流产品在线演示」分类已新增宏达人生条目（指向 `calculator-hongda.html`），并配置了预览占位图 `🧮`。
- **入口 B — 快捷测算 / AI 计划书助手**：`ai-planner.js` 已新增宏达人生产品（`params: ['term','ge']`），弹窗新增「业务类型」选择（非个养 / 个人养老金），跳转 URL 已拼装 `ge` 参数；并在 `parseExtraParams` 中补齐「个养 / 个人养老金 / 非个养」自然语言识别，纯文本输入也能识别业务类型。

### 2. 计算口径
- 继续严格沿用原 Excel（`SHA 03ea4eed...`）逻辑，未自创公式。经查 `S34/S341` 仅影响「是否个养」入口，不影响利益金额；`#REF!`/失效 IRR 不做任何修正或猜测。
- 关键字段：`份数 = 年交保费 / 1000`（数据按每份 1000 元归一化）；`K3 = 红利实现率`，`K4 = 累积生息利率 0.0175`。

### 3. 下载图片重做
- 网页演示表与下载长图共用同一套列结构（12 列：保单年度 / 年龄(年末) / 当年保险费 / 现金价值① / 当年年金 / 累计年金② / 当年红利 / 红利累积生息③ / 年金+红利返还比例 / 身故金 / 预期生存总利益 1+2+3 / 当年增值率），同一套年份筛选规则（均调用 `display-rules.js`）。
- 参考图（ab1e4c634...）的表格结构已对应重建，未擅自删列。

## 三、统一「利益演示年份显示规则」

新建 `display-rules.js` 作为唯一年份抽样数据源，已被所有在线演算器引用：

| 产品类别 | 规则 | 实现 |
|----------|------|------|
| 短期险（盈满鑫、宏禧来） | 全部保单年度逐年显示，不抽行 | `SHORT_TERM` → `'all'` |
| 长期险 投保年龄 < 50 岁 | 60 岁前逐年，60 岁后每 5 年 | `5y60` |
| 长期险 投保年龄 ≥ 50 岁 | 70 岁前逐年，70 岁后每 5 年 | `5y70` |
| 所有产品 | 最后一年（满期/合同终止年）强制显示 | `filterDisplayRows` 内置 |

适配清单：
- `ai-planner.js` 的 `filterRowsByAge`（覆盖 fusheng/hongtai/hongkun/hengxiang/huacai）已补「末行强制显示」。
- `calculator-hongyu.html`（自带 `filterRowsByAge` 覆盖）两处均补「末行强制显示」。
- `calculator-hongan/hongyuan/hongxilai/yingmanxin` 内联过滤统一改为调用 `display-rules.js`，并接入「按投保年龄自动默认显示模式」（≤49→5y60，≥50→5y70；短期险恒 `all`），用户手动切换后保留其选择。
- 宏达人生 `calculator-hongda.html` 早已接入并复用同一套逻辑。

> 规则只作用于展示层，所有现金价值、年金、红利、身故金、生存总利益、JSON/Excel 原始数据均未改动。

## 四、验证结果

`hongda-task/reconcile.py` 复跑（逐年度 × 逐字段）：

- **默认场景 vs Excel 真值**：最大字段差异 = `0.0000`（全部为 0）。
- **12 组结构 / 边界 / 满期金一致性**：全部通过（OK）。
- **关键节点抽样**：首年 / 缴费末年 / 末年 数值与 Excel 一致。

计算逻辑未被本轮显示层改动影响。

## 五、待办 / 回归

- [ ] 人工在浏览器中确认：桌面 / 390×844 移动端 / 下载长图 三视角渲染（本机无 headless 浏览器，需你这边打开 `calculator-hongda.html` 目测）。
- [ ] 宏达人生四档测试（30 岁 / 49 岁临界 / 50 岁 / 58 岁 / 60 岁以上）建议逐一过一遍。
- [ ] 现有演算器（盈满鑫 / 宏禧来 / 宏安 / 宏愿 / 宏御 / 福盛 / 弘泰 / 弘坤 / 华彩 / 恒祥）回归，确认年份显示无遗漏。
- [ ] 经授权后执行 `git commit` → `git push`，并确认 GitHub Pages 重新构建上线。

## 六、交付物路径

- 演算器：`E:\workbuddyFIle\腾讯龙虾的成品\xinhua-hub\calculator-hongda.html`
- 数据源：`E:\workbuddyFIle\腾讯龙虾的成品\xinhua-hub\data_hongda.json`
- 统一规则：`E:\workbuddyFIle\腾讯龙虾的成品\xinhua-hub\display-rules.js`
- 对账脚本与材料：`E:\workbuddyFIle\腾讯龙虾的成品\xinhua-hub\hongda-task\`
