# 项目上下文 · xinhua-hub（未来 AI 接手必读）

> 本文件写给**未来任何接管本项目**的 AI（含跨账号的小龙虾 / 大龙虾 / 腾讯龙虾）。
> 读完即可直接继续开发，**无需追问历史**。
> 配套的**强制规则**见同目录 `AI-development-rules.md`（改代码前必读）。
> 最后更新：2026-08-06，基于仓库实际状态（最新 commit `cf9ef71`，2026-08-05 23:48，线上已生效）。

---

## 1. 项目目标

- 新华保险资料库网站，面向**招行私行渠道**的银保从业人员，提供产品资料 + 在线演算器 + 销售辅助。
- 微信内置浏览器（X5 内核）直接可看，无需下载 App；Chrome 桌面端同样可用。
- 核心价值：把保险产品资料（条款 / 红利实现率 / 增值服务 / 产品对比）和**在线利益演算器**集中成一个"渠道销售随时可查、可一键生成计划书图片发给客户"的工具。
- 关键约束：**手机端体验 > 图片导出体验 > PC 端体验**；**网页效果 = 下载图片效果**。

## 2. 当前版本号

- **设计系统 V4.0**：2026-08-05 建立（`assets/css/theme.css` 设计 token + `assets/css/calc-common.css`）；V4.0 导出 PNG 视觉优化已完成。
- **内容基线 V1.5**：2026-07-31 合并 `main` 上线（红利深度解读、话术中性化、渠道标签通用化等）。
- 无 git tag、无语义版本号；版本以"Vx.x 设计系统"和"Vx.x 内容"描述。
- 最新 commit：`cf9ef71`（2026-08-05 23:48，归档盈满鑫储备期修复验收图），已 push。
- 线上地址：https://1131921527-alt.github.io/xinhua-hub/ （⚠️ `github.io` 微信内被拦截，验收用 Chrome 桌面端）

## 3. 已完成模块

`data.json` 共 14 个分类入口，对应实际页面如下：

| 分类 id | 模块 | 实际文件 / 说明 |
|---|---|---|
| company-intro | 公司介绍 | `company-intro.html`（含中投/中央汇金大白话） |
| product-calculators | 产品计算器 | 10 款 `calculator-*.html` 入口 |
| sales-qa | 销售问答中心 | `sales-qa.html`（三大板块：产品知识库 / 客户常见问题 / 销售话术规范；由 `product-qa-handbook.html` 合并而来，当前唯一入口） |
| bonusrate | 红利实现率 | `dividend.html`（数据查询工具）+ `hongli-realization.html`（图文长文） |
| knowledge-card | 制式卡文字版 | `card-hongyu.html`（宏御问答卡微信直读） |
| product-intro | 产品介绍/解读 | 含 `intro-yingmanxin.html` 等一页通 |
| product-compare | 产品对比 | 产品对比模块 |
| product-rules | 产品规则（条款） | `clause-*.html` 文字版（5 份）+ 官方 PDF |
| workflow | 流程 | 双录 / 适当性等 |
| vip-service | 增值服务 | `zengzhi.html`（和睦家医疗体检权益） |
| training | 培训中心 | `training.html` + `assets/data/training.json`（数据驱动渲染） |
| templates | 制式模板 | — |
| contracts | 合同相关 | — |
| dividend | 分红资料 | — |
| excel-tables | Excel 预览 | 走 MS Office Online Viewer，手机需双指缩放，**非移动适配**（已知短板） |

## 4. 当前稳定功能

- **10 款在线演算器**：页面加载即显示完整演示表（无需先点按钮），手机端免滑动。
- **导出计划书 PNG**：`html2canvas` 截 `#planArea`（盈满鑫用独立 `#exportRoot`），手机/微信弹预览长按保存；**网页效果 = 下载图片效果**。
- **条款文字版**：移动端翻页 / 目录跳转 / 字号± / 进度条，微信 X5 直读。
- **分类标签横滑**：桌面拖拽 + 滚轮，两侧渐隐提示。
- **V4.0 导出图标准**：表格文字垂直居中、统一深蓝网格边框（`1px #1E40AF`）、无多余收益摘要卡。
- **销售问答中心**：合规问答（无收益率数字）与内部解读（带数字）互补分层。

## 5. 已知问题 / 待办（头号风险标 ⚠️）

- ⚠️ **【头号】其余演算器数据数值未与官方演示表逐项核对**：宏御 / 宏愿 / 华彩 / 福盛 / 恒享 / 宏坤等的 `CALC_DATA_INLINE` 可能仍是旧口径（历史根因"错到姥姥家"= 数据缩放/口径错误）。头号待办 = 找官方 Excel 演算表逐项核对替换。**只换正确数据，不改算法。**
- 红利实现率数据仅种子（宏耀世家 1 条），需从官网全量抓取 + 建周期同步自动化（CORS 限制需代理/CloudStudio）。
- 条款 PDF 缺失：宏坤 / 福盛 / 宏安 / 红安世家等尚未上传（现仅 5 份：华彩/宏愿/宏泰/宏御/恒享）。
- Excel 预览非移动适配（桌面版表格，手机需双指缩放）。
- **中行版（`deploy-blue` 目录 + `boc-*.html`）当前工作区不存在**：招行版已移除"第二年末现价百分比"统计项；如需中行版需从 git 历史 / 分支恢复。
- 宏愿 = S02 仍为推断，待用户最终确认（未擅改其他关联）。
- 利率（演示利率/分红率）按最新口径核对未完成。

## 6. 下一步优化方向

1. 核对并修复其余演算器数据（头号，见 §5）。
2. 红利实现率全量抓取 + 周期同步自动化。
3. 补全缺失产品条款 PDF。
4. 福盛按小程序模板调整（待用户发模板）。
5. 利率口径更新。
6. 如需中行版，从分支 / 历史恢复 `deploy-blue`。
7. 持续打磨导出图清晰度与微信发送体验。

## 7. 禁止修改事项（红线）

- **业务计算逻辑**：收益率公式、现金价值口径、红利领取方式、定存组合算法一律冻结。改公式属于必须**暂停向用户确认**的红线项。
- **标准版样式基准**：用户认定的"标准版"（深色表头 / 左侧竖排标签 / 每 5 年高亮 / 缴费期合并 / 底部免责 / 副标题格式）不可擅自变体；配色铁律——**"颜色换蓝以外，其他都不变"**。
- **销售真实场景话术**：不能因"合规"删除销售实际在用场景；"正式页面内容"与"销售辅助话术"必须分区（详见 `AI-development-rules.md` 规则七）。
- **整体重构**：禁止"整体重建大文件"式重写，只能最小精准改动（历史教训：失效 `getElementById` 导致表格永久空白）。
- **样式改一处原则**：改品牌色 / 圆角 / 阴影 / 字体 / 间距只改 `theme.css` 的 `:root`；演算器公共样式只在 `calc-common.css`。不逐页内联重复改。
- **不删核心业务文件 / 不可恢复数据**（属四大须暂停确认项之一）。

## 8. 当前视觉标准（V4.0）

- 设计 token 唯一来源：`assets/css/theme.css`（`--primary`/`--bg`/`--radius`/`--shadow`/`--space-*`/`--font-*` + 公共类 `.btn`/`.card`/`.section-title`/`.tag`/`.grid-*`）。
- 演算器主色 `#1E40AF`，强调 `#3B82F6`；增额寿险系深色表头 `#1E3A8A`，养老年金系 `#78350F`。
- **标准版样式基准（不可随意改）**：左侧竖排标签在最左列；每 5 年高亮 + 末年高亮；储备期统一显示"储备期"（招行版禁用"缴费期/投资期/缴费期分X年投入"）；底部统一免责话术；副标题格式固定（增额系"40岁女士 每年100万 缴3年 分红交清增额" / 年金系"46岁女士 每年50万 缴3年 55岁开始领"）；已删除项（displayMode 选择器 / emoji / 旧浮动免责 / 交替行背景 / rate-reserve 独立列）不再复活。
- **导出图标准**：宽度 ≥ 1600px（scale=2）；盈满鑫 4500px（1500 × scale 3）。`maxCanvasPixels` 不够先提上限（如 32M），别改表格布局迁就；表格文字垂直居中、统一深蓝网格边框。
- 中国股市惯例：涨红跌绿。

## 9. 演算器产品状态

> 当前根目录共 **10 款**演算器。V4.0 视觉优化重点排查的是 **5 款**（盈满鑫 / 宏安 / 恒享 / 宏御 / 宏愿），状态如下；其余 5 款为标准版样式已对齐、数据待核。

### 9.1 V4.0 重点五款（已做导出图居中 / 边框 / 去多余卡修复）

| 产品 | 文件 | 算法 / 数据 | 稳定性 | 备注 |
|---|---|---|---|---|
| 盈满鑫两全(分红型) | `calculator-yingmanxin.html` | 独立 `#exportRoot` 导出模板 + 定存组合 + 交清增额/累计生息切换 | ✅ 最活跃，近期重点 | 含 `data_yingmanxin.json` / `_addcv.json`；储备期 rowspan 居中；导出 4500px |
| 宏安世家 | `calculator-hongan.html` | 已有 | ✅ 稳定 | 指标卡 bug 已修（IIFE 移入 generate 体内） |
| 恒享人生 | `calculator-hengxiang.html` | 千元基准，scale 约掉 | ✅ 稳定 | 非分红型；指标卡曾 bug 已修 |
| 宏御世家 | `calculator-hongyu.html` | 取 guaranteed | ✅ 稳定 | `maxCanvasPixels` 已提 32M 保 scale=2；注入 JS 禁选 `} catch` 锚点 |
| 宏愿人生 | `calculator-hongyuan.html` | 沿用 side-stats 布局 | ✅ 稳定 | 产品码 S02（推断待确认）；指标卡 bug 已修 |

### 9.2 其余五款（标准版样式已对齐，数据待核）

| 产品 | 文件 | 状态 | 备注 |
|---|---|---|---|
| 宏泰世家 | `calculator-hongtai.html` | ✅ 模板基准 | 取 cv_guar（保证部分）；数据已换验证版 `hongtai_full` |
| 福盛世家 | `calculator-fusheng.html` | ✅ 稳定 | 曾 NaN bug（r2[7]→r2[3]）已修 |
| 宏坤人生 | `calculator-hongkun.html` | ✅ 稳定 | 千元基准 |
| 华彩鎏金 | `calculator-huacai.html` | ✅ 稳定 | 取保证部分 |
| 宏禧来 | `calculator-hongxilai.html` | ⚠️ 用户明确不加 | 保留文件但**不作为上线产品** |

> ⚠️ 全部 10 款数据数值与官方演示表逐项核对仍是头号待办（见 §5）。

## 10. 最近一次验收结果

- **验收对象**：V4.0 视觉优化（盈满鑫专题 + 全站 5 款排查），commit `47b9379`（已 push；最终收尾 commit `cf9ef71` 归档验收图）。
- **范围**：6 页面（首页 / 培训中心 / 销售问答 × PC / 手机）+ 5 款 × 3 档（10万 / 100万 / 1000万）导出 PNG。
- **结果**：
  - 6 页面横向滚动全 OK（无溢出）；
  - 5 款 × 3 档导出 PNG **全部 0 网页溢出 / 0 导出溢出**；
  - 盈满鑫导出尺寸 1600px 宽（删顶部三收益卡后高度 2798 / 2738 / 2680px）；
  - 其余四款导出 2000px 宽；
  - 微信模拟图 `_1000w_wechat.png` 每款已生成；
  - 宏安 / 宏愿网页指标卡数字 bug 修复（原显示"—"）。
- **验收报告**：`qa/daily-report/2026-08-05-v4-final-fix-report.md`
- **截图**：`qa/screenshots/2026-08-05-v4-final/`（41 张）
- **验收脚本**：`qa/capture_acceptance.py`（手机 `390×844` / 桌面 `1280×900`，查横向溢出 + Console）

---

## 附：新 AI 开工三步走

1. 先读 `AI-development-rules.md`（强制规则）和本文件。
2. 改样式只动 `theme.css` / `calc-common.css`；改演算器先理解现有结构，**不重构**。
3. 改完用 `qa/capture_acceptance.py` 验收（PC + 手机 0 溢出、Console 0），按规则八汇报**三要素**（修改文件 / 修改内容 / 是否影响其他页面），写 `qa/daily-report/`，再 `git push origin main`（等 GitHub Pages 约 45 秒重建）。
