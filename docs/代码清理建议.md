# 代码清理建议（V1.0 健康检查）

> ⚠️ **本报告仅生成建议，不执行任何删除 / 修改 / 重构。** 所有结论供人工决策。
> 扫描范围：`xinhua-hub/` 全部文件（2026-07-30）。
> 红线：不改 Excel 数据、不改公式、不重设计储备期、不大规模重构 → 因此以下「建议」均指**未来可选优化**，非本次动作。

---

## 一、重复代码（最大可维护性风险）

| 现象 | 数据 | 影响 |
|---|---|---|
| `function generate()` 在每个演算器各定义 1 次 | 10 个 calculator-*.html 各 1 处，共 **10 份** | 改一处需同步 10 处，极易漏改 |
| `function downloadImage()` 在每个演算器各定义 1 次 | 同上，**10 份** | 同上 |
| 演算器 CSS 大量重复 | 10 个 HTML 共 **6402 行**，估算重复 CSS ~1000+ 行 | 体积大、样式难统一 |
| 其他重复函数 | `fmtMoney / fmtNum / buildTable / renderTable` 等多在多个文件中重复 | 同左 |

**建议（未来重构，非本次）**：抽 `common.js`（generate/downloadImage/格式化/规则解析）+ `common.css`，各演算器 `<script src>` 引用。
**当前状态**：因属「大规模重构」，按红线**本次不动**，仅记录待办。

---

## 二、没有引用的文件（疑似孤儿）

grep 全仓 `.html`/`.js` 后未发现有效引用：

| 文件 | 类型 | 结论 | 建议 |
|---|---|---|---|
| `hongtai_calcdata.js` | JS | 无任何引用 | 可移 `dev-archive/`（保留不删） |
| `hongtai_data.js` | JS | 无任何引用 | 可移 `dev-archive/` |
| `hongtai_calc_data.json` | JSON | 仅 `calculator-hongtai.html` 实际 fetch `hongtai_full.json`，本文件未被引用 | 可移 `dev-archive/` |
| `hongyu_data.json` | JSON | 无任何引用（`calculator-hongyu.html` 数据已内嵌） | 可移 `dev-archive/` |
| `package.json` / `package-lock.json` | JSON | 仅 Node 测试脚本（tests/）依赖，页面不引用 | **保留**（测试需要） |
| `V1.0-health-check.json` | JSON | 为 `tests/health-check.js` 的输出物 | **保留** |

> 说明：前 4 个为真正孤儿；后 2 类为构建/测试必需，勿动。

---

## 三、废弃 JS（死代码）

| 函数 | 出现位置 | 份数 | 状态 |
|---|---|---|---|
| `_legacyDownloadImage` | `calculator-fusheng / hengxiang / hongkun / hongtai / hongyu / huacai` | 定义 6 处、调用 6 处 | **当前流程均走 `html2canvas` 新下载，此旧函数不可达（死代码）** |

**建议（未来可选）**：在不触发回归的前提下删除这 6 处 `_legacyDownloadImage`，可减小体积。
**当前状态**：属代码改动，按「只生成报告」要求**本次不删**。

---

## 四、临时测试 / 生成文件

以下均为**历史快照或一次性脚本**，不应进发布包，但**按约定只归档不删除**：

| 位置 | 内容 | 建议 |
|---|---|---|
| `dev-archive/*.py` | `verify_*.py` `regenerate_*.py` `extract_*.py` 等约 20 个 | 已归档，保持 |
| `archive/*.py` | `gen_*.py` 生成脚本 | 已归档，保持 |
| `archive/calculators-2026-07-27/` `archive/演算器手机测试/` `backup/*` | 历史演算器/布局修复快照 | 已归档，保持 |
| `tools/演算器Excel生成脚本/` | html-v2/v3/v4 等多版生成器 | 开发工具，保持 |
| `.workbuddy/verify-calculator-screenshots.js` | 内部校验脚本 | 保持 |

---

## 五、综合建议优先级

1. **P2（可立即做，低风险）**：把 4 个孤儿文件移入 `dev-archive/`（仅移动，不删）。
2. **P3（需回归测试）**：删除 6 处 `_legacyDownloadImage` 死代码。
3. **P1（高价值但属重构，排期外）**：抽 `common.js` / `common.css` 消除 10 份重复。

> 所有项均需在你点头后才可执行；本报告阶段**零改动**。
