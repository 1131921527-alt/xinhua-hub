# 华彩演算器迁移 · 确认清单（待拍板）

生成时间：2026-08-13
依据：`data_huacai.draft.json`（官方全量表 39,524 条）+ 官方 Excel 投保信息/利益演示页

## 一、分红方式口径（关键确认项）

官方资料明确写死为 **累积生息**：

- 投保信息页：「红利领取方式 **累积生息**」
- 利益演示页：「假设红利领取方式选择为 **累积生息**。」

即官方全量表给出的红利数值，就是 **累积生息** 口径，没有交清增额版本。

## 二、当前页面的问题

`calculator-huacai.html` 有「累积生息 / 交清增额」下拉，但代码并未按选项切换数据或公式（两种都走同一套基准数据）。迁移后应：

- 使用官方「累积生息」数据（`data_huacai.draft.json` 的 `div_annual` / `div_cum`）；
- 删除 `ageOffset` 只调年龄列不调值的伪逻辑；
- 「交清增额」选项需确认：是移除，还是另行补交清增额官方表。

## 三、需要你确认的一点

- 分红方式：是否 **仅保留累积生息**（与官方模板一致），把「交清增额」选项移除或置灰？还是你手头有交清增额的官方数据，需要我一并接入？

确认后我把 `data_huacai.draft.json` 接进页面，替换 35 岁单基准。

## 补充说明

官方全量表数值列完整（无占位），迁移工程量小；主要就是确认分红方式选项如何处理。
